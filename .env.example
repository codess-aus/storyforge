# AI Foundry / Azure AI Configuration (Primary text/chat models)
AZURE_AI_ENDPOINT=your-text-endpoint-here
AZURE_AI_API_KEY=your-text-api-key-here

# Optional: Separate endpoint for image model if deployed in a different region/resource
# If gpt-image-1 had to be deployed elsewhere, fill these. Otherwise leave blank.
AZURE_IMAGE_ENDPOINT=your-image-endpoint-here
AZURE_IMAGE_API_KEY=your-image-api-key-here

# GitHub Configuration (for storing stories)
GITHUB_TOKEN=your-github-token-here
GITHUB_REPO=codess-aus/storyforge

# Optional: Azure Storage for images (future)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string-here
